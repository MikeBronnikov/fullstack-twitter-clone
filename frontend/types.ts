export enum LoadingEnum {
    loading = 'loading',
    loaded = 'loaded',
    failed = 'failed',
    never = 'never'
    }

export enum RoutesEnum {
    start = '/',
    home = '/home',
    search='/search'
}