import { ResponseInterface } from './../globalTypes';
import  express from 'express';
class _AuthController {
    async login (req: express.Request, res: express.Response<ResponseInterface>){
        
    }
}